// @comma-agents/rlprompter — reinforced prompt generation.
//
// The package barrel re-exports the headless engine so it can be consumed
// programmatically. The interactive TUI is launched via the `rlprompter` bin
// (src/main.tsx).

export * from "./engine";
