import type { StrategyOption } from "./StrategyPicker";

/**
 * @deprecated Use `discoverStrategies()` from `../strategy-discovery` instead.
 * Fallback used when discovery produces no results.
 */
export const BUILT_IN_STRATEGIES: readonly StrategyOption[] = [];
