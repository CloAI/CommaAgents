export { useRegion } from "./useRegion";
export type {
  RegionDimensions,
  RegionHandle,
  RegionOptions,
  RegionPosition,
} from "./useRegion.types";

export {
  buildRegionOutput,
  CURSOR_RESTORE,
  CURSOR_SAVE,
  cursorTo,
  fitToWidth,
  getAbsolutePosition,
  padToWidth,
  visibleLength,
} from "./useRegion.utils";
