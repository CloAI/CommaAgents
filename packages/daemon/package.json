{
  "name": "@comma-agents/daemon",
  "version": "2.0.0",
  "description": "Agent daemon — executes flows and exposes WebSocket API",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test"
  },
  "dependencies": {
    "@comma-agents/core": "workspace:*"
  },
  "author": "CloAI",
  "license": "MIT"
}
