# @comma-agents/daemon

Background execution daemon and WebSocket protocol for CommaAgents.

```bash
bun add --global @comma-agents/daemon@next
comma-agents-daemon start
```

Requires Bun 1.3 or newer.
