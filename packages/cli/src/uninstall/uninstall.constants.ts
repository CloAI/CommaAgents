export const HISTORY_DATA_ENTRIES = ["runs"] as const;

export const PACKAGE_DATA_ENTRIES = [
  "packages",
  "hub",
  "providers",
  "cache",
  "provider-registry.json",
] as const;

export const CLI_PACKAGE_NAME = "@comma-agents/cli";
