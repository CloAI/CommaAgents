---
title: Credentials
description: Credential store, backends, and management types.
---

# Credentials

The daemon manages API keys and other credentials through a pluggable credential store with backend support.

## CredentialStore

The credential store interface for managing secrets.

<AutoTypeTable path="../packages/core/src/credentials/credentials.types.ts" name="CredentialStore" />

## CredentialBackend

Backend interface for persisting credentials (e.g. JSON file, OS keychain).

<AutoTypeTable path="../packages/core/src/credentials/credentials.types.ts" name="CredentialBackend" />

## CreateCredentialStoreOptions

Options for `createCredentialStore`.

<AutoTypeTable path="../packages/core/src/credentials/credentials.types.ts" name="CreateCredentialStoreOptions" />
